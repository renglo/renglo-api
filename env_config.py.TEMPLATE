WL_NAME='x'

BASE_URL = 'x'
# Cloud console URL (Amplify) — CORS and production invite links when INVITE_FE_BASE_URL is unset
FE_BASE_URL = 'x'
# Local dev console URL for invite email links (overrides FE_BASE_URL). Match Vite port in console/vite.config.ts
INVITE_FE_BASE_URL = 'http://127.0.0.1:5174'
DOC_BASE_URL = 'x'
# Application SES from-address (must match verified SES identity from bootstrap)
FROM_EMAIL = 'noreply@your-app-domain.com'
AWS_REGION = 'x'

# EventBridge (role used to invoke the ingress API Destination)
ROLE_ARN = 'x'

# Local scheduler (dev/ebe). Cloud EventBridge stays on; Schedule UI Local mode talks to ebe.
EVENTBRIDGE_EMULATOR_URL = 'http://127.0.0.1:5056'


# DynamoDB
DYNAMODB_ENTITY_TABLE = ''
DYNAMODB_BLUEPRINT_TABLE = ''
DYNAMODB_RINGDATA_TABLE = ''
DYNAMODB_REL_TABLE = ''
DYNAMODB_CHAT_TABLE = ''
DYNAMODB_GRAPH_TABLE = ''
DYNAMODB_SEARCH_TABLE = ''
DYNAMODB_SESSION_TABLE = ''

# GraphDB
GRAPH_DB_ENABLED = True


CSRF_SESSION_KEY = ''
SECRET_KEY = ''


# flask_cognito
COGNITO_REGION = 'x'
COGNITO_USERPOOL_ID = 'x'
COGNITO_APP_CLIENT_ID = 'x'
COGNITO_CHECK_TOKEN_EXPIRATION = True  # Optional, default is True


# UI
PREVIEW_LAYER = 2


#-----------

S3_BUCKET_NAME = ''

#---------

# OPEN AI
OPENAI_API_KEY=''

#---------

# WEB SOCKET
WEBSOCKET_CONNECTIONS=''

#--------
ALLOW_DEV_ORIGINS = True


#--------
# External Handlers Configuration
# Comma-separated list of extensions that have external handlers
EXTERNAL_HANDLERS = ''
EXTERNAL_HANDLERS_USE_DEV_DOCKER = ''

#--------
# EventBridge → API universal ingress. Connection/API Destination send
# X-Renglo-Ingress-Secret on POST /_schd/ingress.
RENGLO_INGRESS_SECRET = ''
# Public webhook edge base from Stack B (Meta etc. → /{portfolio}/{org}/{channel})
WEBHOOK_EDGE_BASE_URL = ''
# Shared EventBridge API Destination used by webhooks and scheduled jobs
RENGLO_INGRESS_DESTINATION = ''

#--------
# Gmail extension — platform Google OAuth client (Asana/cos-demo style).
# Create once in GCP (External Web client recommended for any-domain agent inboxes).
# Tenants only click Connect; they do not paste client credentials.
GOOGLE_OAUTH_CLIENT_ID = ''
GOOGLE_OAUTH_CLIENT_SECRET = ''
# Optional override; default is {BASE_URL}/_schd/gmail/oauth_callback
GMAIL_OAUTH_REDIRECT_URI = ''
# Optional; falls back to AUTH_SECRET / SECRET_KEY for OAuth state HMAC
OAUTH_STATE_SECRET = ''

