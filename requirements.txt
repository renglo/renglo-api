# Renglo API Dependencies

# Core Library (use editable local path for development, git URL for production)
# Development: changes in ../renglo-lib are picked up without reinstall (Flask debug reload)
-e ../renglo-lib
# Production: renglo-lib @ git+https://github.com/yourorg/renglo-lib@v1.0.0

# Flask and extensions (some overlap with renglo-lib, pip will resolve)
Flask==3.1.0
Flask-Cors==5.0.0
Flask-Cognito==1.21
Flask-Caching==2.1.0

# AWS
boto3==1.43.72
apig-wsgi==2.18.0

# Deployment
zappa==0.59.0
setuptools>=45.0.0,<81

# Dev: reliable Flask auto-reload for editable packages and extensions
watchdog>=4.0.0